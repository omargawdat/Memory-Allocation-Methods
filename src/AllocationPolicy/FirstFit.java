package AllocationPolicy;

import Memory.Partition;

import java.util.Vector;

public class FirstFit implements IAllocator {
    @Override
    public void sort(Vector<Partition> partitions) {
        // Keep Vector as it is
    }
}
